# 线程池

线程池模块基于工作队列解耦 Reactor 线程与业务执行线程。

## 设计要点

- 主线程或 SubReactor 将任务投递到工作队列
- 工作线程竞争获取任务并执行
- 处理模型为半同步/半反应堆
- 支持同步 I/O 场景下的任务分发
- 热路径上的任务队列采用有界无锁 MPMC 队列
- 线程等待任务时使用信号量阻塞，避免队列读写走互斥锁
- 线程扩缩容仍保留，扩容相关元数据更新使用低频互斥保护
- 通过 `threadpool_queue_mode` 可切换 `mutex` 和 `lockfree` 两种模式




